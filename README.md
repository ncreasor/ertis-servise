# Ertis Service - Система управления заявками ЖКХ

FastAPI бэкенд с AI-анализом проблем, автоматическим распределением заявок и полной документацией Swagger.

## 🚀 Основные возможности

### Для пользователей:
- ✅ Регистрация и вход в систему
- 📸 Создание заявок с фотографиями проблем
- 🗺️ Автокомплит адресов через Яндекс.Карты
- 📊 Просмотр статуса своих заявок
- ⭐ Оценка работы сотрудников

### Для сотрудников ЖКХ:
- 👷 Личный кабинет с назначенными заявками
- ✅ Завершение заявок с фото решения
- 📈 Отслеживание своего рейтинга

### Для администраторов ЖКХ:
- 👥 Управление сотрудниками
- 📋 Просмотр всех заявок с фильтрами
- 📊 Подробная статистика
- ⚙️ Назначение заявок вручную

### AI-функционал:
- 🤖 **GPT-4o-mini**: Анализ текстового описания проблемы
- 🔍 **GPT-4o**: Анализ фотографии и определение приоритета (1-5)
- 🎯 **Автоматическое распределение**: AI выбирает подходящего сотрудника

---

## 📋 Требования

- **Python 3.8+**
- **MySQL 5.7+** или **MySQL 8.0+**
- **OpenAI API ключ** (для AI-анализа)
- **Яндекс.Карты API ключ** (для автокомплита адресов)

---

## 🛠️ Установка и настройка

### 1. Клонирование репозитория

```bash
cd "Ertis Service"
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate  # для macOS/Linux
# или
venv\Scripts\activate  # для Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка базы данных MySQL

Создайте базу данных:

```sql
CREATE DATABASE ertis_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'ertis_user'@'localhost' IDENTIFIED BY 'your_password';
GRANT ALL PRIVILEGES ON ertis_db.* TO 'ertis_user'@'localhost';
FLUSH PRIVILEGES;
```

### 5. Настройка переменных окружения

Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# Application
APP_NAME="Ertis Service"
APP_VERSION="1.0.0"
DEBUG=True
ENVIRONMENT=development

# Server
HOST=0.0.0.0
PORT=8000

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Security
SECRET_KEY=your-very-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Database
DATABASE_URL=mysql+aiomysql://ertis_user:your_password@localhost:3306/ertis_db

# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key-here

# File Storage
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760

# Yandex Maps API
YANDEX_MAPS_API_KEY=your-yandex-maps-api-key-here

# Logging
LOG_LEVEL=INFO
```

### 6. Запуск приложения

**Способ 1: Через скрипт (рекомендуется)**

```bash
./start.sh
```

**Способ 2: Напрямую через Python**

```bash
python run.py
```

**Способ 3: Через uvicorn**

```bash
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

При первом запуске автоматически:
- Создадутся все таблицы в БД
- Добавятся категории проблем и специальности
- Создастся директория `uploads/` для файлов

---

## 📚 Документация

После запуска сервера документация доступна по адресам:

- **Swagger UI**: http://localhost:8000/api/docs
- **ReDoc**: http://localhost:8000/api/redoc
- **OpenAPI JSON**: http://localhost:8000/api/openapi.json
- **[Подробная документация API](./API_DOCUMENTATION.md)**

---

## 🗂️ Структура проекта

```
Ertis Service/
├── app/
│   ├── __init__.py
│   ├── main.py                     # Главный файл приложения
│   ├── api/
│   │   └── v1/
│   │       ├── router.py           # Главный роутер API
│   │       ├── auth.py             # Аутентификация
│   │       ├── requests.py         # Заявки
│   │       ├── employees.py        # Сотрудники
│   │       ├── categories.py       # Категории
│   │       ├── statistics.py       # Статистика
│   │       ├── addresses.py        # Автокомплит адресов
│   │       └── health.py           # Health check
│   ├── core/
│   │   ├── config.py               # Настройки
│   │   ├── database.py             # Подключение к БД
│   │   ├── security.py             # JWT, пароли
│   │   ├── dependencies.py         # FastAPI dependencies
│   │   └── logging.py              # Логирование
│   ├── models/                     # SQLAlchemy модели
│   │   ├── user.py
│   │   ├── employee.py
│   │   ├── request.py
│   │   ├── category.py
│   │   ├── specialty.py
│   │   ├── rating.py
│   │   └── housing_organization.py
│   ├── schemas/                    # Pydantic схемы
│   │   ├── user.py
│   │   ├── employee.py
│   │   ├── request.py
│   │   ├── category.py
│   │   ├── rating.py
│   │   └── auth.py
│   └── services/                   # Бизнес-логика
│       ├── openai_service.py       # OpenAI интеграция
│       ├── file_service.py         # Работа с файлами
│       └── init_data.py            # Начальные данные
├── uploads/                        # Загруженные файлы
├── logs/                           # Логи приложения
├── .env                            # Переменные окружения
├── .env.example                    # Пример переменных окружения
├── requirements.txt                # Зависимости Python
├── run.py                          # Скрипт запуска
├── start.sh                        # Скрипт для macOS/Linux
├── README.md                       # Этот файл
└── API_DOCUMENTATION.md            # Документация API
```

---

## 🧪 Тестирование API

### Примеры запросов через curl

**1. Регистрация пользователя:**

```bash
curl -X POST http://localhost:8000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "first_name": "Иван",
    "last_name": "Иванов",
    "username": "ivan",
    "email": "ivan@example.com",
    "password": "password123"
  }'
```

**2. Вход:**

```bash
curl -X POST http://localhost:8000/api/v1/auth/login \
  -d "username=ivan&password=password123"
```

**3. Получение категорий:**

```bash
curl http://localhost:8000/api/v1/categories
```

**4. Создание заявки:**

```bash
curl -X POST http://localhost:8000/api/v1/requests \
  -H "Authorization: Bearer {ваш_токен}" \
  -F "description=Прорвало трубу в квартире" \
  -F "address=г. Москва, ул. Ленина, д. 10" \
  -F "category_id=1" \
  -F "photo=@/path/to/photo.jpg"
```

---

## 🎯 Демо-данные

Для создания демо-данных откройте `app/main.py` и раскомментируйте строку:

```python
# await create_demo_data(session)
```

Это создаст:
- **Админ ЖКХ**: `username: admin`, `password: admin123`
- **Пользователь**: `username: user`, `password: user123`
- **Демо организацию ЖКХ**

---

## 🔑 API ключи

### Получение OpenAI API ключа

1. Зарегистрируйтесь на https://platform.openai.com/
2. Перейдите в API keys
3. Создайте новый ключ
4. Добавьте в `.env`: `OPENAI_API_KEY=sk-...`

### Получение Яндекс.Карты API ключа

1. Зарегистрируйтесь на https://developer.tech.yandex.ru/
2. Создайте приложение
3. Подключите JavaScript API и HTTP Геокодер
4. Скопируйте API ключ
5. Добавьте в `.env`: `YANDEX_MAPS_API_KEY=...`

---

## 📊 Категории проблем (автоматически создаются)

1. **Водоснабжение** - Сантехник, Слесарь-сантехник
2. **Электричество** - Электрик, Электромонтер
3. **Дорожное покрытие** - Дорожный рабочий, Асфальтоукладчик
4. **Вывоз мусора** - Дворник, Мусорщик
5. **Уборка территории** - Дворник, Уборщик территории
6. **Лифт** - Лифтер, Механик лифтового оборудования
7. **Благоустройство** - Рабочий по благоустройству, Озеленитель
8. **Отопление** - Слесарь-сантехник, Теплотехник
9. **Подъезд** - Электрик, Слесарь, Дворник

---

## 🔧 Production рекомендации

1. **Измените** `DEBUG=False` в `.env`
2. **Установите надежный** `SECRET_KEY`
3. **Настройте** `ALLOWED_ORIGINS` для вашего фронтенда
4. **Используйте** reverse proxy (nginx, traefik)
5. **Настройте** systemd service или Docker
6. **Используйте** gunicorn для production:

```bash
pip install gunicorn
gunicorn app.main:app -w 4 -k uvicorn.workers.UvicornWorker -b 0.0.0.0:8000
```

---

## 🐛 Логи

Логи сохраняются в `logs/app.log`:
- Ротация при достижении 500 MB
- Хранение 10 дней
- Автоматическая архивация в zip

---

## 📝 Лицензия

MIT

---

## 👨‍💻 Разработано для хакатона

Система управления заявками ЖКХ с AI-анализом проблем и автоматическим распределением задач.

**Технологии:**
- FastAPI
- MySQL
- SQLAlchemy (async)
- OpenAI GPT-4o / GPT-4o-mini
- Яндекс.Карты API
- JWT Authentication
- Pydantic
- Loguru

---

## 🤝 Контакты

Если есть вопросы по использованию - пишите!
