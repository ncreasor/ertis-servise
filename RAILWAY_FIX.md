# Исправление ошибки "ModuleNotFoundError: No module named 'MySQLdb'" на Railway

## Проблема

Railway автоматически создает `DATABASE_URL` в неправильном формате для асинхронного подключения.

## Быстрое решение (2 минуты)

### Шаг 1: Открой Variables в Railway

1. Зайди в свой проект на Railway
2. Перейди во вкладку **"Variables"**

### Шаг 2: Исправь DATABASE_URL

1. Найди переменную `DATABASE_URL`
2. Нажми на иконку **карандаша** (Edit)
3. Измени **только префикс** с `mysql://` на `mysql+aiomysql://`

**Пример:**

❌ **БЫЛО** (неправильно):
```
mysql://root:abc123@containers-us-west-123.railway.app:6543/railway
```

✅ **СТАЛО** (правильно):
```
mysql+aiomysql://root:abc123@containers-us-west-123.railway.app:6543/railway
```

### Шаг 3: Сохрани и дождись redeploy

1. Сохрани изменения
2. Railway автоматически запустит redeploy (2-3 минуты)
3. Проверь логи - ошибка должна исчезнуть

---

## Проверка

После успешного деплоя открой:

```
https://твой-проект.up.railway.app/api/docs
```

Должна открыться Swagger документация без ошибок.

---

## Если не помогло

### Проверь что все переменные установлены:

```env
DATABASE_URL=mysql+aiomysql://root:pass@host:port/db  ✅ правильный префикс!
OPENAI_API_KEY=sk-...
SECRET_KEY=длинная-строка
DEBUG=False
ALLOWED_ORIGINS=https://твой-фронт.com
```

### Проверь логи Railway:

1. Railway Dashboard → Deployments → Latest Deployment
2. Нажми **"View Logs"**
3. Ищи другие ошибки

---

## Альтернативное решение (если всё равно не работает)

Можно вручную добавить переменную с правильным форматом:

1. Railway → Variables → **+ New Variable**
2. Имя: `DATABASE_URL`
3. Значение: `mysql+aiomysql://ВАШ_URL_ЗДЕСЬ` (скопируй из MySQL Reference Variable и исправь префикс)

---

## MySQL Reference Variable

В Railway есть специальная переменная от MySQL сервиса (например `${{MySQL.DATABASE_URL}}`).

**Не используй её напрямую!** Она в неправильном формате.

Вместо этого:
1. Посмотри её значение
2. Скопируй
3. Создай новую переменную `DATABASE_URL` с исправленным префиксом

---

## Готово!

Теперь бэк должен запуститься без ошибок 🚀
