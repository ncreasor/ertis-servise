# Деплой на Railway.app - Пошаговая инструкция

## Шаг 1: Подготовка проекта

Все необходимые файлы уже созданы:
- ✅ `Procfile` - команда запуска для Railway
- ✅ `requirements.txt` - зависимости Python
- ✅ `.env.example` - пример переменных окружения

## Шаг 2: Загрузка на GitHub

1. Создай репозиторий на GitHub (можно private)

2. Инициализируй git в проекте:
```bash
cd "Ertis Service"
git init
git add .
git commit -m "Initial commit - Ertis Service backend"
```

3. Подключи удаленный репозиторий:
```bash
git remote add origin https://github.com/твой-username/твой-репозиторий.git
git branch -M main
git push -u origin main
```

## Шаг 3: Создание проекта на Railway

1. Зайди на https://railway.app/

2. Нажми **"Start a New Project"**

3. Выбери **"Deploy from GitHub repo"**

4. Авторизуй Railway для доступа к GitHub

5. Выбери свой репозиторий

6. Railway автоматически обнаружит Python и начнет деплой

## Шаг 4: Добавление MySQL базы данных

1. В проекте Railway нажми **"+ New"** → **"Database"** → **"Add MySQL"**

2. Railway создаст БД и автоматически добавит переменную `DATABASE_URL`

3. **ВАЖНО:** Railway использует формат `mysql://...`, а нам нужен `mysql+aiomysql://...`

4. Скопируй значение `DATABASE_URL` и измени в настройках проекта:
   ```
   Было: mysql://root:password@containers-us-west-1.railway.app:6789/railway
   Стало: mysql+aiomysql://root:password@containers-us-west-1.railway.app:6789/railway
   ```

## Шаг 5: Настройка переменных окружения

В разделе **"Variables"** твоего Railway проекта добавь:

### Обязательные переменные:

```env
# Database (Railway создаст автоматически, но исправь префикс)
DATABASE_URL=mysql+aiomysql://root:password@host:port/database

# OpenAI
OPENAI_API_KEY=sk-твой-ключ-openai

# Security
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Application
APP_NAME=Ertis Service
APP_VERSION=1.0.0
DEBUG=False
ENVIRONMENT=production

# Server (Railway автоматически устанавливает PORT)
HOST=0.0.0.0

# CORS (добавь домен своего фронтенда)
ALLOWED_ORIGINS=https://твой-фронтенд.vercel.app,https://твой-домен.railway.app

# File Storage
UPLOAD_DIR=uploads
MAX_FILE_SIZE=10485760

# Logging
LOG_LEVEL=INFO
```

### Опциональные переменные:

```env
# Yandex Maps API (если нужен автокомплит адресов)
YANDEX_MAPS_API_KEY=твой-ключ-яндекс
```

## Шаг 6: Деплой

1. После добавления всех переменных Railway автоматически запустит redeploy

2. Дождись успешного деплоя (статус "Success")

3. Получи публичный URL:
   - Нажми **"Settings"** → **"Generate Domain"**
   - Railway создаст домен типа `твой-проект.up.railway.app`

## Шаг 7: Проверка

1. Открой `https://твой-проект.up.railway.app/api/docs`

2. Должна открыться Swagger документация

3. Проверь эндпоинт `/api/v1/health` - должен вернуть OK

4. Попробуй зарегистрировать пользователя через `/api/v1/auth/register`

## Шаг 8: Инициализация данных

При первом запуске автоматически создадутся:
- ✅ Все таблицы в БД
- ✅ 9 категорий проблем
- ✅ Специальности для категорий

Проверь через `/api/v1/categories` - должны вернуться категории.

---

## Troubleshooting

### Проблема: Railway не может установить зависимости

**Решение:** Проверь что `requirements.txt` в корне проекта

### Проблема: Ошибка подключения к БД

**Решение:**
1. Проверь что в `DATABASE_URL` префикс `mysql+aiomysql://`, а не просто `mysql://`
2. Убедись что MySQL сервис запущен в Railway

### Проблема: 500 Internal Server Error

**Решение:**
1. Проверь логи: Railway → Deployments → Latest Deployment → View Logs
2. Убедись что все переменные окружения установлены
3. Проверь что `OPENAI_API_KEY` валиден

### Проблема: CORS ошибки

**Решение:** Добавь домен фронтенда в `ALLOWED_ORIGINS`:
```env
ALLOWED_ORIGINS=https://твой-фронт.vercel.app,https://другой-домен.com
```

---

## Автоматический Redeploy при пуше в GitHub

Railway автоматически делает redeploy при каждом пуше в main ветку:

```bash
git add .
git commit -m "Update backend"
git push origin main
```

Через 2-3 минуты изменения появятся на проде.

---

## Полезные команды

### Просмотр логов
Railway Dashboard → Deployments → View Logs

### Откат на предыдущую версию
Railway Dashboard → Deployments → Выбери старый деплой → Redeploy

### Изменение переменных окружения
Railway Dashboard → Variables → Edit

---

## Production checklist

- [ ] `DEBUG=False` в переменных окружения
- [ ] Валидный `OPENAI_API_KEY`
- [ ] `DATABASE_URL` с правильным префиксом `mysql+aiomysql://`
- [ ] `ALLOWED_ORIGINS` содержит домен фронтенда
- [ ] `SECRET_KEY` - случайная длинная строка (не дефолтная)
- [ ] MySQL сервис запущен в Railway
- [ ] Публичный домен сгенерирован
- [ ] Swagger доступен по `/api/docs`
- [ ] Категории создались (проверь через API)

---

## Твой бэкенд будет доступен по адресу:

```
https://твой-проект.up.railway.app
```

### Документация API:
```
https://твой-проект.up.railway.app/api/docs
```

### Эндпоинты:
```
POST   https://твой-проект.up.railway.app/api/v1/auth/register
POST   https://твой-проект.up.railway.app/api/v1/auth/login
GET    https://твой-проект.up.railway.app/api/v1/categories
POST   https://твой-проект.up.railway.app/api/v1/requests
...
```

Удачи на хакатоне! 🚀
